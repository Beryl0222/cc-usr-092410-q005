{
  "name": "digital-exhibit-governance",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "领域事件基础校验",
  "scripts": {
    "test": "node --test",
    "build": "node --check src/validator.js && node --check src/event-store.js && node --check src/projections.js && node --check src/retirement-service.js"
  }
}
